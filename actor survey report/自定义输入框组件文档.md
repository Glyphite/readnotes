# CustomOutlinedTextField 自定义输入框组件文档

## 概述

`CustomOutlinedTextField` 是一个基于 Jetpack Compose 的自定义输入框组件，提供了 Material Design 3 风格的轮廓输入框样式。该组件在 Android 应用中用于用户文本输入，具有良好的视觉效果和交互体验。

## 组件特性

- **Material Design 3 风格**：遵循最新的 Material Design 设计规范
- **焦点状态管理**：自动处理焦点状态下的视觉变化
- **自定义样式**：支持自定义文本样式、内边距等属性
- **标签动画**：标签在获得焦点或有内容时会自动上移并变色
- **主题适配**：自动适配应用的颜色主题

## 完整代码

```kotlin
package com.glyphwrite.core.presentation.component.custom

import androidx.compose.foundation.interaction.MutableInteractionSource
import androidx.compose.foundation.interaction.collectIsFocusedAsState
import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.foundation.text.BasicTextField
import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.OutlinedTextFieldDefaults
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.runtime.getValue
import androidx.compose.runtime.remember
import androidx.compose.ui.Modifier
import androidx.compose.ui.graphics.SolidColor
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.input.VisualTransformation
import androidx.compose.ui.unit.dp

@Composable
fun CustomOutlinedTextField(
    value: String,
    onValueChange: (String) -> Unit,
    label: String,
    modifier: Modifier = Modifier,
    singleLine: Boolean = true,
    maxLines: Int = 1,
    minLines: Int = 1,
    enabled: Boolean = true,
    textStyle: TextStyle = MaterialTheme.typography.bodySmall,
    contentPadding: PaddingValues = PaddingValues(horizontal = 12.dp, vertical = 8.dp)
) {
    val interactionSource = remember { MutableInteractionSource() }
    val isFocused by interactionSource.collectIsFocusedAsState()
    val isError = false // 假设没有错误状态

    BasicTextField(
        value = value,
        onValueChange = onValueChange,
        modifier = modifier,
        singleLine = singleLine,
        maxLines = maxLines,
        minLines = minLines,
        textStyle = textStyle.copy(
            color = MaterialTheme.colorScheme.onSurface
        ),
        cursorBrush = SolidColor(MaterialTheme.colorScheme.primary),
        interactionSource = interactionSource,
        decorationBox = { innerTextField ->
            OutlinedTextFieldDefaults.DecorationBox(
                value = value,
                visualTransformation = VisualTransformation.None,
                innerTextField = innerTextField,
                singleLine = singleLine,
                enabled = true,
                isError = isError,
                label = {
                    Text(
                        text = label,
                        style = MaterialTheme.typography.bodySmall,
                        color = if (isFocused || value.isNotEmpty()) {
                            MaterialTheme.colorScheme.primary
                        } else {
                            MaterialTheme.colorScheme.onSurfaceVariant
                        }
                    )
                },
                placeholder = null,
                leadingIcon = null,
                trailingIcon = null,
                prefix = null,
                suffix = null,
                supportingText = null,
                colors = OutlinedTextFieldDefaults.colors(
                    focusedBorderColor = MaterialTheme.colorScheme.primary,
                    unfocusedBorderColor = MaterialTheme.colorScheme.outline,
                    focusedLabelColor = MaterialTheme.colorScheme.primary,
                    unfocusedLabelColor = MaterialTheme.colorScheme.onSurfaceVariant,
                    focusedTextColor = MaterialTheme.colorScheme.onSurface,
                    unfocusedTextColor = MaterialTheme.colorScheme.onSurface
                ),
                contentPadding = contentPadding,
                interactionSource = interactionSource
            )
        }
    )
}
```

## 参数详解

### 必需参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
| `value` | `String` | 输入框的当前文本值 |
| `onValueChange` | `(String) -> Unit` | 文本变化时的回调函数 |
| `label` | `String` | 输入框的标签文本 |

### 可选参数

| 参数名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `modifier` | `Modifier` | `Modifier` | 组件的修饰符 |
| `singleLine` | `Boolean` | `true` | 是否为单行输入 |
| `maxLines` | `Int` | `1` | 最大行数 |
| `minLines` | `Int` | `1` | 最小行数 |
| `enabled` | `Boolean` | `true` | 是否启用输入框 |
| `textStyle` | `TextStyle` | `MaterialTheme.typography.bodySmall` | 文本样式 |
| `contentPadding` | `PaddingValues` | `PaddingValues(horizontal = 12.dp, vertical = 8.dp)` | 内容内边距 |

## 核心逻辑分析

### 1. 焦点状态管理

```kotlin
val interactionSource = remember { MutableInteractionSource() }
val isFocused by interactionSource.collectIsFocusedAsState()
```

- 使用 `MutableInteractionSource` 来跟踪用户的交互状态
- `collectIsFocusedAsState()` 将焦点状态转换为 State，便于 Compose 重组
- 焦点状态用于控制标签颜色和边框颜色的变化

### 2. 基础文本输入框

```kotlin
BasicTextField(
    value = value,
    onValueChange = onValueChange,
    // ... 其他参数
)
```

- `BasicTextField` 是 Compose 中最基础的文本输入组件
- 提供了文本输入的核心功能，但不包含装饰性元素
- 通过 `decorationBox` 参数添加装饰性元素

### 3. 装饰性盒子

```kotlin
decorationBox = { innerTextField ->
    OutlinedTextFieldDefaults.DecorationBox(
        // ... 装饰性配置
    )
}
```

- `DecorationBox` 提供了 Material Design 风格的装饰性元素
- 包括边框、标签、颜色等视觉效果
- `innerTextField` 参数代表实际的文本输入区域

### 4. 标签动态颜色

```kotlin
label = {
    Text(
        text = label,
        style = MaterialTheme.typography.bodySmall,
        color = if (isFocused || value.isNotEmpty()) {
            MaterialTheme.colorScheme.primary
        } else {
            MaterialTheme.colorScheme.onSurfaceVariant
        }
    )
}
```

- 当输入框获得焦点或有内容时，标签颜色变为主题色
- 否则使用 `onSurfaceVariant` 颜色
- 这种颜色变化提供了良好的视觉反馈

### 5. 颜色主题配置

```kotlin
colors = OutlinedTextFieldDefaults.colors(
    focusedBorderColor = MaterialTheme.colorScheme.primary,
    unfocusedBorderColor = MaterialTheme.colorScheme.outline,
    focusedLabelColor = MaterialTheme.colorScheme.primary,
    unfocusedLabelColor = MaterialTheme.colorScheme.onSurfaceVariant,
    focusedTextColor = MaterialTheme.colorScheme.onSurface,
    unfocusedTextColor = MaterialTheme.colorScheme.onSurface
)
```

- 完全遵循 Material Design 3 的颜色规范
- 使用 `MaterialTheme.colorScheme` 确保与主题一致
- 分别配置了焦点和非焦点状态的各种颜色

## 使用示例

### 基本用法

```kotlin
@Composable
fun ExampleUsage() {
    var text by remember { mutableStateOf("") }
    
    CustomOutlinedTextField(
        value = text,
        onValueChange = { text = it },
        label = "请输入用户名"
    )
}
```

### 自定义样式

```kotlin
@Composable
fun CustomStyledExample() {
    var text by remember { mutableStateOf("") }
    
    CustomOutlinedTextField(
        value = text,
        onValueChange = { text = it },
        label = "多行输入",
        singleLine = false,
        maxLines = 5,
        textStyle = MaterialTheme.typography.bodyMedium,
        contentPadding = PaddingValues(16.dp)
    )
}
```

## 设计优势

### 1. **一致性**
- 遵循 Material Design 3 规范
- 与应用整体主题保持一致
- 提供统一的用户体验

### 2. **可定制性**
- 支持多种自定义参数
- 可以轻松适应不同的使用场景
- 灵活的样式配置

### 3. **性能优化**
- 使用 `remember` 缓存交互源
- 避免不必要的重组
- 高效的状态管理

### 4. **可访问性**
- 支持焦点状态管理
- 提供清晰的视觉反馈
- 符合无障碍设计原则

## 扩展建议

### 1. 错误状态支持

```kotlin
// 添加错误状态参数
isError: Boolean = false,
errorMessage: String? = null

// 在组件中使用
val isError = isError || (errorMessage != null)
```

### 2. 密码输入支持

```kotlin
// 添加密码模式参数
isPassword: Boolean = false,

// 在 decorationBox 中添加
visualTransformation = if (isPassword) PasswordVisualTransformation() else VisualTransformation.None
```

### 3. 前缀和后缀支持

```kotlin
// 添加图标参数
leadingIcon: @Composable (() -> Unit)? = null,
trailingIcon: @Composable (() -> Unit)? = null,

// 在 DecorationBox 中使用
leadingIcon = leadingIcon,
trailingIcon = trailingIcon
```

## 总结

`CustomOutlinedTextField` 是一个设计良好、功能完整的自定义输入框组件。它基于 Jetpack Compose 的现代架构，提供了 Material Design 3 风格的用户界面，具有良好的可扩展性和可维护性。通过合理的参数设计和状态管理，该组件能够满足大多数文本输入场景的需求，同时保持代码的简洁性和可读性。
